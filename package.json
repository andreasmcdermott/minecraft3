{
  "name": "minecraft3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev.ts",
    "build": "bun build ./src/index.html --outdir ./dist --target browser --minify"
  },
  "dependencies": {
    "three": "0.170.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.0",
    "@types/three": "0.170.0"
  },
  "engines": {
    "bun": "1.3.0"
  }
}
